from distutils.core import setup

setup(name="animator",
      packages=["animator"],
      version=0.1,
      url="https://github.com/gujoh/animator",
      install_requires=["matplotlib"],
      include_package_data=True)
